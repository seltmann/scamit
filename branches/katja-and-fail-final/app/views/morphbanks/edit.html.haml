%h1.heading
  = "Editing morphbank %s" % @morphbank.id
  
- semantic_form_for(@morphbank) do |form|
  = render 'form', :form => form
  - form.buttons do
    = form.commit_button 'Update'
    
%p.actions
  = link_to 'Cancel', morphbanks_path