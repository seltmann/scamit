%h1.heading
  = 'New Locality'
  
- semantic_form_for(@locality) do |form|
  = render 'form', :form => form
  - form.buttons do
    = form.commit_button 'Create'
    
%p.actions
  = link_to 'Cancel', localities_path