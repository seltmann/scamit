.welcomeFont
  = 'New Synonym'
#contentNoSidebar
  %h1
  .leftMargin
    %br
    %br
    - semantic_form_for(@synonym) do |form|
      = render 'form', :form => form
      - form.buttons do
        = form.commit_button 'Create'
    
    %p.actions
      = link_to 'Cancel', synonyms_path