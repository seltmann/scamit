%h1.heading
  = "Editing upload %s" % @upload.id
  
- semantic_form_for(@upload) do |form|
  = render 'form', :form => form
  - form.buttons do
    = form.commit_button 'Update'
    
%p.actions
  = link_to 'Cancel', uploads_path